{
  "name": "mygenesis",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel serve src/index.html",
    "console": "tsc && node build/console.js",
    "build": "cross-env NODE_ENV=production parcel build src/index.html --public-url .",
    "watch": "parcel watch src/index.html",
    "predeploy": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "world": "node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash-es": "^4.17.21",
    "typescript": "^4.7.3"
  },
  "directories": {
    "build": "build"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "parcel": "^2.6.0",
    "parcel-bundler": "^1.12.5"
  }
}
